spring:
  redis:
    host: redis
    port: 6379

  h2:
    console:
      enabled: true
      path: /h2-console

  datasource:
    hikari:
      driver-class-name: com.mysql.cj.jdbc.Driver
      jdbc-url: jdbc:mysql://localhost:3306/wow_town?serverTimezone=UTC&characterEncoding=UTF-8?enabledTLSProtocols=TLSv1.2
      username: root
      password: wowTown123

  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

  decorator:
    datasource:
      p6spy.enable-logging: true